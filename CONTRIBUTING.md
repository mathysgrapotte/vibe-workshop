please run `uv venv` to create a virtual environment and install the dependencies.

> you'll need to install [uv](https://github.com/astral-sh/uv) manually.
>
> You can install it with:
>
> ```bash
> curl -LsSf https://astral.sh/uv/install.sh | sh
> ```
>

You now have the dependencies installed.

